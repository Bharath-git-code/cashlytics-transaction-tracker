# Minimal Dockerfile for Kivy APK building
FROM kivy/buildozer:latest

# Set working directory
WORKDIR /app

# Copy project files
COPY . .

# Make sure we have the latest buildozer and accept licenses
RUN yes | buildozer android list || true

# Build APK
CMD ["buildozer", "android", "debug"]